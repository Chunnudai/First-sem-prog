#include <stdio.h>
int main ()
{
    int n,i,temp,fact,rem,sum=0;
    printf("Enter the number\n");
    scanf("%d",&n);
    temp=n;
    while(n)
    {
        i=1,fact =1;
        rem = fact*i;
    }

}